{
  "name": "@banou26/libav-wasm",
  "version": "0.1.7",
  "main": "build/index.js",
  "files": [
    "build"
  ],
  "types": "build/index.d.ts",
  "type": "module",
  "scripts": {
    "dev": "npm i ./dist && concurrently \"npm run dev-wasm\" \"npm run dev-web\"",
    "dev-wasm": "nodemon -e cpp --exec \"npm run make-docker\"",
    "make-docker": "docker-compose run libav-wasm make",
    "dev-web": "vite --port 4560",
    "vite-build": "vite build",
    "build": "npm run copy-package && npm run make-docker && npm i ./dist && vite build && npm run types && npm run copy-wasm && npm remove libav",
    "types": "tsc",
    "copy-package": "copyfiles -u 2 ./src/build-config/package.json dist",
    "copy-wasm": "copyfiles -u 1 dist/*.wasm build && shx mv ./build/index2.js ./build/worker.js && shx mv ./build/worker/index.d.ts ./build/worker.d.ts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^24.0.1",
    "concurrently": "^7.6.0",
    "copyfiles": "^2.4.1",
    "nodemon": "^2.0.20",
    "shx": "^0.3.4",
    "typescript": "^4.9.4",
    "vite": "^4.0.4"
  },
  "dependencies": {
    "mp4box": "^0.5.2",
    "osra": "^0.0.8",
    "p-queue": "^7.3.0"
  }
}

namespace Transmuxer;
 
enum Operation:byte {
  Idle = 0,
  Seek = 1,
  Read = 2
}

table Interface {
  // state: State;
  operation: Operation;
  
  // argument types
  // seek and read
  arg_offset: int;
  // seek
  arg_whence: int;
  // read
  arg_buffer_size: int;

  // return types
  // read
  buffer: [uint8];
  // seek
  offset: int;
}

root_type Interface;

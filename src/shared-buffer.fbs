namespace Transmuxer;
 
enum Operation:byte {
  Seek = 0,
  Read = 1,
  Write = 2
}

table Interface {
  state: int32;
  operation: Operation;
  arg_offset: long;
  arg_whence: int;
  arg_buffer_size: int;
  buffer: [uint8];
}

root_type Interface;
